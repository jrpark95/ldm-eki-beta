# ==============================================================================
# LDM Deposition Mesh Configuration
# ==============================================================================
# This file defines the output mesh grid for dry/wet deposition accumulation.
# The mesh covers a rectangular region in geographic coordinates (lat/lon).
#
# Source location reference: 129.4761°E, 35.7131°N (from source.txt)
#
# Format: key = value (spaces around '=' are optional)
# Lines starting with '#' are comments
# ==============================================================================

# Grid boundaries (degrees)
# The mesh should cover the expected plume extent
start_lon = 129.0    # Western boundary [degrees East]
end_lon   = 131.2    # Eastern boundary [degrees East]
start_lat = 34.7     # Southern boundary [degrees North]
end_lat   = 36.0     # Northern boundary [degrees North]

# Grid resolution (degrees)
# Smaller values = higher resolution = more grid points
# 0.05 degree ~ 5.5 km at this latitude
lon_step = 0.02      # Longitude resolution [degrees]
lat_step = 0.02      # Latitude resolution [degrees]

# ==============================================================================
# With these settings:
#   - Domain: 11° × 10° (lon × lat)
#   - Resolution: 0.05° (~5.5 km)
#   - Grid size: 221 × 201 = 44,421 points
#   - Memory: ~174 KB per deposition array (dry/wet)
# ==============================================================================
